package constant

const WholePercent = 100
const (
	StatisticTitle = "Statistic"
	StatisticPath  = "/statistic"
)

const StatisticIntroduction = "Tokens each memory takes up in the model's context, not the size of this page. Block is the rendered block that enters context when a memory is loaded whole - as an always memory, or when the relevance search matches it. Description is the prose the index line carries."

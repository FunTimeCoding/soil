package keeper

// Kept document.
const kept = "golf" // kept trailing

// Moved document.
const moved = "hotel" // moved trailing
// Keeper vocabulary.
const (
	// Delta document.
	delta = "india" // delta trailing

	epsilon = "juliet"
)
